import java.net.*;
import java.io.*;
import java.util.Arrays;

public class Client {
    public static void main(String args[]){
        int serverPort = 7000; // porta do servidor
        ClientSocket clientSocket = new ClientSocket(serverPort);

        /*
        AddressBook.Builder addressBook = AddressBook.newBuilder();

        addressBook.setId(Integer.valueOf(678));
        addressBook.setName("Codrigo Rampiolo");
        addressBook.setEmail("crampiolo@iworst.com.br");

        byte[] buffer = addressBook.toByteArray();
        */

    }
}